{
  "name": "projects-backend",
  "version": "1.0.0",
  "description": "Backend for Projects",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development nodemon server.js",
    "dev": "cross-env NODE_ENV=development nodemon server.js --watch controllers --watch models --watch routes --watch middleware",
    "import-projects": "cross-env NODE_ENV=development node scripts/import-projects.js",
    "migrate-users": "cross-env NODE_ENV=development node scripts/update-user-schema.js",
    "debug": "cross-env NODE_ENV=development nodemon --inspect server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "csvtojson": "^2.0.10",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.6.3",
    "twilio": "^5.5.2"
  },
  "devDependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.5.0",
    "morgan": "^1.10.0",
    "nodemon": "^3.1.10"
  }
}
